import styled from 'styled-components';

import Header from './header';

const Home = (props) => {

    return (
        
        <div>
            <Header />
            <h2>home</h2>
        </div>
    )
};

export default Home;